# 矢量网络分析仪领域

本上下文描述 VNA 如何组织测量、采集、显示与校准。它用于统一产品、算法、前端、后端和驱动开发中的业务语言。

## 仪表与测量组织

**仪表（Instrument）**：
一台可配置、可触发并产生测量结果的 VNA 逻辑实体，也是状态和硬件能力的归属边界。
_Avoid_: 设备实例、主机

**通道（Channel）**：
一组共享频率轴、激励、触发、平均和校准绑定的测量上下文。同一通道内可合并采集需求相容的多个测量。
_Avoid_: 窗口、曲线组

**测量（Measurement）**：
对被测网络中某个物理量的定义，例如 S11、S21 或接收机比值；它描述“测什么”，不包含显示形式。
_Avoid_: 曲线、Trace、显示格式

**S 参数测量（S-parameter Measurement）**：
`Sij` 表示激励端口 `j` 时，响应端口 `i` 的出射波 `b[i]` 与激励参考波 `a[j]` 之比。共享同一激励端口的多个 S 参数应合并进同一个激励状态并共享原始接收机数据；二端口或多端口只改变端口组合与硬件执行计划，不产生彼此独立的数据链。
_Avoid_: S11 专用链路、每条 Trace 单独采集

**迹线（Trace）**：
一个测量结果的显示视图，定义格式、比例、数学运算和样式；多个迹线可以引用同一个测量。
_Avoid_: Measurement、扫频任务

**迹线格式（Trace Format）**：
迹线将测量结果映射到显示坐标和单位的方式，例如对数幅度、相位或 Smith 图；
改变格式不改变测量定义，也不增加采集。
_Avoid_: Measurement Type、扫频配置

**显示比例（Scale）**：
迹线显示坐标的数值范围，由每格增量、参考值和参考位置共同确定；单位随迹线
格式确定。显示比例属于 Trace 的显示状态，不是测量结果或采集配置。
_Avoid_: Sweep Range、CSS 缩放

**窗口（Window）**：
承载一个或多个迹线的显示布局容器，不拥有测量配置，也不触发采集。
_Avoid_: Channel、Measurement

**显示工作区（Display Workspace）**：
仪表中全部窗口、迹线及其布局关系的集合。
_Avoid_: 仪表状态、测量计划

## 扫频与采集

**扫频（Sweep）**：
按照一个已确定的测量计划完成的一次采集过程，从硬件准备开始，到完整测量帧可供消费时结束。
_Avoid_: 页面刷新、Trace 更新

**连续扫频（Continuous Sweep）**：
仪表在每次扫频完成后自动启动下一次扫频的运行状态。
_Avoid_: 前端轮询

**单次扫频（Single Sweep）**：
由一次触发请求启动并只产生一个完整结果的扫频。
_Avoid_: 单点测量

**扫频计划（Sweep Plan）**：
为得到通道中启用的测量集合而确定的频率轴、激励状态、接收机需求和触发条件。
_Avoid_: Trace 列表、驱动命令列表

**激励状态（Source State）**：
扫频计划中的一个源端口及其功率、频率和路径配置；一次扫频可以包含多个激励状态。
_Avoid_: Measurement、端口配置页面

**原始接收机帧（Raw Receiver Frame）**：
一次采集产生的参考接收机和测试接收机复数样本及其质量信息，尚未完成测量合成和系统误差修正。
_Avoid_: S 参数帧、显示曲线

**网络测量帧（Network Frame）**：
由原始接收机数据合成并完成适用校准修正的复数网络测量结果。
_Avoid_: 原始帧、屏幕像素数据

**迹线帧（Trace Frame）**：
按照迹线定义从网络测量帧派生的结果，可用于显示、标记和极限判断。
_Avoid_: 原始帧、Measurement

## 校准与夹具

**校准套件（CalKit）**：
一组具有已知电气模型和连接器属性的校准标准件定义。
_Avoid_: CalSet、校准结果

**校准标准件（Calibration Standard）**：
校准套件中的一个已知参考对象，例如 Open、Short、Load 或 Through。
_Avoid_: 被测件、CalSet

**校准会话（Calibration Session）**：
从校准规划、标准件采集到求解和验证的一次有状态工作过程。
_Avoid_: CalSet、校准算法

**校准集（CalSet）**：
一次已完成校准的不可变、可版本化结果，包含适用条件、误差项、来源数据和验证结果。
_Avoid_: CalKit、可编辑校准配置

**校准绑定（Calibration Binding）**：
通道与一个特定版本校准集之间的使用关系。
_Avoid_: CalSet 副本

**夹具链（Fixture Chain）**：
连接在仪表参考面与被测件之间、需要进行端口延伸或去嵌处理的网络序列。
_Avoid_: CalKit、硬件端口路径

## 结果分析

**标记（Marker）**：
附着于迹线数据的分析游标，用于读取指定位置或执行峰值、带宽等搜索。
_Avoid_: 图形注释

**极限测试（Limit Test）**：
将完整分辨率的迹线数据与预定义边界进行比较并产生通过或失败结论的分析。
_Avoid_: 显示裁剪

**被测件（DUT）**：
VNA 当前测量的网络对象。
_Avoid_: 校准标准件、夹具
