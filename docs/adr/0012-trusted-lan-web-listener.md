# ADR-0012：可信局域网 Web 监听
本决定取代总体架构中“非回环 Web 必须先启用 TLS”的阶段约束：`vna-server` 监听 `0.0.0.0:8080`，让本机和可信局域网设备共享同一 REST/WebSocket 入口。浏览器使用 `127.0.0.1`、主机名或服务器实际 IP，命令 ID 使用明文 LAN 页面仍可用的 `crypto.getRandomValues()`。

当前决定不包含 TLS、身份认证或公网加固，服务不得直接暴露到公网或不可信网络。
