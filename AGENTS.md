# AGENTS.md

本文件用于约束智能编码代理的行为。目标是减少不必要的改动、避免过度设计、提高变更可验证性，并保证提交记录清晰可靠。

## 核心原则

### 1. 先思考，再编码

在实现前必须明确问题、假设和成功标准。

- 不要隐式假设需求。
- 不要隐藏不确定性。
- 存在多种理解时，应说明差异，而不是静默选择其中一种。
- 存在更简单方案时，应主动说明。
- 需求不清晰时，应停止实现，指出不清晰之处并向用户询问。
- 对多步骤任务，应先给出简短计划，并为每一步定义验证方式。

推荐格式：

```text
1. [步骤] → 验证：[检查方式]
2. [步骤] → 验证：[检查方式]
3. [步骤] → 验证：[检查方式]
```

### 2. 简单优先

只实现当前任务需要的最小代码。

- 不添加未被要求的功能。
- 不为单次使用场景创建抽象。
- 不添加未被要求的灵活性、配置项或扩展点。
- 不为空想中的异常场景添加复杂错误处理。
- 如果实现明显可以更短、更清晰，应先简化。

判断标准：资深工程师是否会认为这次实现过度复杂。若会，应重写为更简单方案。

### 3. 外科手术式修改

只修改完成任务所必需的内容。

- 不顺手重构无关代码。
- 不修改无关注释、格式或命名。
- 不清理与本任务无关的旧代码。
- 应匹配现有代码风格，即使个人偏好不同。
- 若发现无关问题或死代码，只在回复中说明，不直接修改。
- 若本次改动造成新的无用导入、变量、函数或文件，应同步移除。
- 不删除本来就存在但与任务无关的无用代码，除非用户明确要求。

判断标准：每一行变更都应能直接追溯到用户需求。

## 代码规模与职责约束

- 函数应保持单一职责，不得超过 50 行，入参不得超过 5 个。
- 单个函数内的嵌套深度不超过5层。
- 单个文件不得超过 250 行。
- Git 提交应保持小而聚焦，单次提交的代码变更行数不得超过 500 行。
- 仅在极端且确有必要的情况下可以违反上述限制。违反前必须说明原因、影响范围、已考虑的替代方案和风险，并经用户明确审核确认；未经确认不得违反。


## Git 规范

本项目使用 Git 进行版本管理。

- 新项目创建仓库时，必须补全 `.gitignore`。
- 不提交与项目无关的文件。
- 每完成一次任务，应进行一次 Git 提交。
- Commit 信息必须使用中文。
- Commit 描述应规范、清晰，包含标题和修改内容摘要。

推荐格式：

```text
<类型>：<简短标题>

- <修改摘要 1>
- <修改摘要 2>
- <验证方式>
```

示例：

```text
修复：修正用户输入校验逻辑

- 增加空字符串与非法格式校验
- 补充对应单元测试
- 已运行 pytest tests/test_validation.py
```

## 文档维护

代码改动完成后，应同步检查是否需要更新 `README.md`。

- 行为、命令、配置、安装步骤或使用方式发生变化时，必须更新 `README.md`。
- 只更新与本次变更直接相关的文档内容。
- 不顺手重写或美化无关文档段落。

## 多 Agent 长期协作工作流

本项目把 Agent 视为长期团队成员，而不是只负责某一个提交的临时执行器。主协调 Agent 负责项目目标、优先级、任务拆分、冲突管理、主线集成与最终验收；专业 Agent 可随项目阶段承接其职责范围内的任何任务。

### 通用角色

建议长期保留以下角色，名称可以调整，但职责必须明确：

- **产品与架构 Agent**：澄清用户目标，维护领域与接口契约，设计跨层纵向切片，检查是否过度设计。
- **应用与协议 Agent**：实现应用服务、命令编排、HTTP/SCPI 等外部协议和对应公共接口测试。
- **平台与数据 Agent**：实现数据源、设备适配、持久化、日志、并发基础设施和跨平台能力。
- **前端与体验 Agent**：实现前端数据接入、交互、可视化、浏览器验收和用户可见文档。

测试策略、双轴审核、构建复验和主线集成由主协调 Agent 负责，也可以将独立的只读复核临时分配给空闲专业 Agent。

角色是默认责任范围，不是硬隔离。当某项工作独立、无文件冲突且某 Agent 空闲时，主协调 Agent 应主动调整分工。

### 固定 Agent 对话

派工时必须优先复用以下长期对话。对话 ID 是稳定标识，显示名称可以由用户调整：

| 通用角色 | 对话 ID |
| --- | --- |
| 产品与架构 Agent | `019fb71a-3fa2-7be0-af61-77606cb15402` |
| 应用与协议 Agent | `019fb706-7b1a-7463-9219-0828161778db` |
| 平台与数据 Agent | `019fb706-16bc-7693-95d9-8f62808acfc7` |
| 前端与体验 Agent | `019fb709-e33c-7dd1-b688-032d48a9c426` |

除非用户明确要求创建新 Agent，主协调 Agent 不得用临时子 Agent 替代上述长期对话。派工前通过任务列表核验对话 ID 与工作树状态；即使显示名称发生变化，也按 ID 继续使用同一 Agent。

### 优先级原则

1. 先定义当前最重要的**用户可见结果**，再安排内部模块。
2. 优先完成最小端到端纵向切片，例如“数据产生 → 服务端传输 → 前端显示”，而不是连续堆叠暂时不可见的基础设施。
3. 只有当基础设施直接阻塞当前纵向切片，或存在明确安全/数据损坏风险时，才可优先处理基础设施。
4. 每个阶段都必须有可由用户观察或由真实公共接口验证的完成标准。

### 调度流程

1. 主协调 Agent 维护一个按用户价值排序的任务队列，明确当前里程碑、依赖关系和验收标准。
2. 开工前，主协调 Agent 检查已有长期 Agent；除非用户明确要求，不重复创建临时子 Agent。
3. 将可并行且不修改同一文件的工作同时分派。共享文件必须提前声明单一写入者，其他 Agent 只读审计。
4. 每个 Agent 在独立 worktree/分支工作，先核验基线和工作树状态，不 reset、rebase 或覆盖其他 Agent 的修改。
5. Agent 以小型纵向提交推进：设计或红测 → 最小实现 → 聚焦测试 → 完整相关测试 → 审核 → 中文提交。
6. Agent 完成提交后可以等待集成，但主协调 Agent 必须及时复验、集成并分配下一项独立工作；不得让多个 Agent 因遗漏调度长期空闲。
7. 主协调 Agent 派工后不能仅报告“已安排”就停止；应继续处理主线集成、验收、冲突消解，或持续等待并汇总 Agent 结果，直到当前里程碑取得实质进展或确实需要用户决策。

### 门禁与连续推进

- 外部接口、状态机、数据格式或高风险并发语义必须先经过设计门禁。
- 普通且范围清晰的实现可由主协调 Agent 一次性预先批准文件范围、测试要求和提交权限，减少重复等待。
- Agent 遇到范围扩大、共享文件冲突、超过规模限制、破坏性操作或需求歧义时必须停止并回报。
- 只读审核 Agent 不修改代码。审核发现必须由原实现 Agent 或主协调 Agent 修正。
- 未通过测试或验收的代码不得描述为“已完成”；已编码但未集成的功能必须明确标注为“待集成”。

### 进度汇报

对用户使用通俗语言，固定说明：

- 已经能用的功能；
- 正在实现的用户可见功能；
- 尚未实现且会影响体验的关键缺口；
- 各 Agent 正在负责什么，以及停止或等待的具体原因；
- 下一步按什么顺序推进。

不得用内部类名、测试数量或提交数量代替产品进度。必要时可补充技术细节，但应先说明用户能看到什么。

## 工作完成标准

任务完成前，应确认以下事项：

- 已按需求完成最小必要改动。
- 未引入未请求的功能或抽象。
- 未修改无关代码、格式或文档。
- 已清理本次改动产生的无用代码。
- 已执行相关测试或说明无法执行的原因。
- 已根据需要更新 `README.md`。
- 已遵守文件删除安全限制。
- 已使用中文提交规范完成 Git commit。

## 成效判断

这些规则的目标是实现以下结果：

- 更少的不必要 diff。
- 更少的过度设计。
- 更少因误解需求导致的返工。
- 更清晰的验证过程。
- 更可靠、更可追踪的提交历史。
