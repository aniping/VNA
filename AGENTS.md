# VectorNetworkAnalyzer 项目指令

本文件只保存本仓库独有的长期约束。通用工程纪律、规模限制、Git 规范、文档维护和完成标准属于当前 Codex 用户的全局配置，不作为本仓库可移植的重复真值。

## 产品平台架构门禁

`docs/product-platform-vision.md` 记录用户最初设想的产品级架构。它高于阶段计划和当前实现，但低于用户当前明确决定。

- 设计或实施大需求前，必须完整阅读该总纲、`docs/architecture.md`、`CONTEXT.md` 和相关 ADR。
- “大需求”包括跨两个以上层级，或改变公共合同、状态机、数据链、所有权、部署方式、用户默认流程的工作。
- 完成大需求后，必须按总纲第 9 节执行架构回看，并在交付说明中报告偏离情况。
- 当前代码、测试或阶段性方案不能反向覆盖产品总纲；发现冲突必须停止并向用户说明。
- 改变五层职责、四种场景、双核心流程、业务真值、硬件替换或跨平台目标前，必须取得用户明确确认并同步 ADR 与总纲。
- 具体库、类名、文件布局、线程和编码格式属于可演进实现，不得误写成永久产品原则。

## ZNB 功能规范与页面 UI 门禁

`docs/znb-conformance-gate.md` 是所有用户可见功能和页面的强制完成标准。

- 参考优先级为：用户当前明确指令与截图、ZNB User Manual v74、用户确认的 ZNB 实机证据、仅作补充的 ZNA v41、现有实现。
- 功能开工前必须记录对应手册章节和页码、入口、默认值、输入、状态转换、数据影响、错误、UI 合同与验收步骤；手册不明确时停止并询问用户。
- 功能语义、菜单路径、启用状态和页面布局必须严格复现 ZNB；没有真实业务行为的按钮或只有后端接口的半套功能不得标记完成。
- 未实现功能必须隐藏或原生禁用；部分实现必须列出差距，不能以近似功能冒充。
- 完成时必须同时通过功能规范审核、自动化测试和正式 release 的真实浏览器 UI 验收；测试不能替代实机目视与交互。
- 任何偏离必须在编码前列出手册原行为、拟变更、原因和影响，取得用户明确确认并记录。
- 修改 `frontend/` 前还必须完整阅读并遵守 `frontend/AGENTS.md`。

## 用户价值与工程门禁

- 先定义当前最重要的用户可见结果，再安排内部模块；优先交付“数据产生 → 服务端传输 → 前端显示”等最小端到端纵向切片。
- 只有基础设施直接阻塞当前纵向切片，或存在明确安全、并发或数据损坏风险时，才可优先处理基础设施。
- 外部接口、状态机、数据格式、所有权或高风险并发语义必须先经过设计门禁。
- 每个阶段都必须有用户可观察或可由真实公共接口验证的完成标准。
- 未通过测试或验收的代码不得描述为“已完成”；已编码但未集成的功能必须明确标注为“待集成”。

## 长期 Agent 协作

参与或协调本项目的长期 Agent 工作，包括派工、实施、审核、巡检、看板和主线集成前，必须完整阅读 `docs/agent-collaboration.md`。

- 必须复用该文档登记的四个固定对话；除非用户明确要求，不得创建同职责的新 Agent、临时子 Agent 或其他任务替代它们。

## 本项目附加完成条件

除全局完成标准外，交付前还必须确认：

- 大需求已完成产品平台架构回看；所有偏离已经用户确认并记录。
- 涉及 ZNB 功能或页面时，已通过功能规范与页面 UI 1:1 门禁。
- 用户可见纵向切片已通过正式发布产物和真实公共入口验证；需要实机浏览器验收时不得以开发服务器或 Mock 代替。
