# AGENTS.md

本文件用于约束智能编码代理的行为。目标是减少不必要的改动、避免过度设计、提高变更可验证性，并保证提交记录清晰可靠。

## 适用范围

- 本文件适用于项目中的所有代码修改、文档修改、测试、依赖安装、环境管理和 Git 操作。
- 若存在更具体的项目级、目录级或任务级说明，应在不违反安全约束的前提下与本文件合并执行。
- 对于简单任务，可在保证正确性的前提下适当简化流程，但不得跳过安全限制和验证要求。

## Windows / PowerShell 约束

当前环境是 Windows 11，默认 shell 为 PowerShell 7（`pwsh`）。

- PowerShell 7 与 Windows PowerShell 5.1 是不同产品。描述或调用默认 shell 时必须明确使用 PowerShell 7（`pwsh`）；仅在任务明确要求兼容 5.1 时才使用 `powershell.exe`。
- 默认使用 PowerShell 语法；除非明确处于 Linux、WSL 或 Bash 环境，否则不要使用 Bash 专用语法。
- 字面字符串和正则优先使用单引号；仅在需要变量插值时使用双引号。
- 当命令同时涉及复杂正则、单双引号和多层转义时，优先拆成多个简单命令或先赋值给变量。
- 执行多行 Python 时禁止使用 Bash heredoc；使用 PowerShell here-string，并通过 `| python -` 或项目规定的 Python 启动命令执行。
- `foreach`、`if` 等语句块需要进入管道时，先用 `$()` / `@()` 包裹，或先赋值给变量。
- 使用 `rg` 时，搜索根目录应为真实路径。文件名过滤优先使用 `-g`；含通配符的目录应先用 `Get-ChildItem -Filter` 展开，再传递真实路径。
- 搜索代码优先使用 `rg` 和 `rg --files`；仅在 `rg` 不可用时改用 PowerShell 原生命令。

## 项目信息

> 新项目应在初始化时补全本节信息。

- Python 环境管理工具：Conda
- Conda 环境名称：待补充

## 核心原则

### 1. 先思考，再编码

在实现前必须明确问题、假设和成功标准。

- 不要隐式假设需求。
- 不要隐藏不确定性。
- 存在多种理解时，应说明差异，而不是静默选择其中一种。
- 存在更简单方案时，应主动说明。
- 需求不清晰时，应停止实现，指出不清晰之处并向用户询问。
- 对多步骤任务，应先给出简短计划，并为每一步定义验证方式。

推荐格式：

```text
1. [步骤] → 验证：[检查方式]
2. [步骤] → 验证：[检查方式]
3. [步骤] → 验证：[检查方式]
```

### 2. 简单优先

只实现当前任务需要的最小代码。

- 不添加未被要求的功能。
- 不为单次使用场景创建抽象。
- 不添加未被要求的灵活性、配置项或扩展点。
- 不为空想中的异常场景添加复杂错误处理。
- 如果实现明显可以更短、更清晰，应先简化。

判断标准：资深工程师是否会认为这次实现过度复杂。若会，应重写为更简单方案。

### 3. 外科手术式修改

只修改完成任务所必需的内容。

- 不顺手重构无关代码。
- 不修改无关注释、格式或命名。
- 不清理与本任务无关的旧代码。
- 应匹配现有代码风格，即使个人偏好不同。
- 若发现无关问题或死代码，只在回复中说明，不直接修改。
- 若本次改动造成新的无用导入、变量、函数或文件，应同步移除。
- 不删除本来就存在但与任务无关的无用代码，除非用户明确要求。

判断标准：每一行变更都应能直接追溯到用户需求。

### 4. 目标驱动执行

将任务转化为可验证目标，并循环直到验证通过。

示例：

- “添加校验” → 先为非法输入写测试，再让测试通过。
- “修复 Bug” → 先写出能复现 Bug 的测试，再修复并验证。
- “重构 X” → 重构前后都要确保测试通过。

执行时应持续对照成功标准。如果成功标准过于模糊，应先澄清。

## 代码修改规范

- 优先阅读与当前任务直接相关的文件。
- 避免一次性完整阅读全部源码或超长目标文档。
- 采用阶段式工作方式：每阶段只阅读必要文件，并完成一小块明确目标。
- 修改现有代码时，应保持最小 diff。
- 不引入与任务无关的新依赖。
- 不提交生成物、缓存文件、临时文件或与项目无关的文件。

### 代码规模与职责约束

- 函数应保持单一职责，不得超过 50 行，入参不得超过 5 个。
- 单个文件不得超过 250 行。
- Git 提交应保持小而聚焦，单次提交的代码变更行数不得超过 500 行。
- 仅在极端且确有必要的情况下可以违反上述限制。违反前必须说明原因、影响范围、已考虑的替代方案和风险，并经用户明确审核确认；未经确认不得违反。

## 测试与验证

完成代码修改后，应根据项目情况执行相关验证。

- 优先运行与改动相关的最小测试集。
- 若任务影响范围较大，再运行更完整的测试集。
- 若存在 lint、type check 或 build 命令，应根据改动类型执行。
- 若无法运行测试，应说明原因，并给出已完成的替代验证。
- 修复 Bug 时，优先添加或更新能够复现问题的测试。

## Git 规范

本项目使用 Git 进行版本管理。

- 新项目创建仓库时，必须补全 `.gitignore`。
- 不提交与项目无关的文件。
- 每完成一次任务，应进行一次 Git 提交。
- Commit 信息必须使用中文。
- Commit 描述应规范、清晰，包含标题和修改内容摘要。

推荐格式：

```text
<类型>：<简短标题>

- <修改摘要 1>
- <修改摘要 2>
- <验证方式>
```

示例：

```text
修复：修正用户输入校验逻辑

- 增加空字符串与非法格式校验
- 补充对应单元测试
- 已运行 pytest tests/test_validation.py
```

## Python 环境规范

Python 项目必须使用 Conda 进行环境管理。

- 全新 Python 项目应由代理创建新的 Conda 环境。
- 现有项目应从本文件的“项目信息”中读取环境名称。
- 如果无法确定项目对应的 Conda 环境，应向用户询问。
- 安装 Python 依赖失败时，应立即停止，并向用户寻求帮助。
- 不要擅自切换到其他环境管理方式。

## 文档维护

代码改动完成后，应同步检查是否需要更新 `README.md`。

- 行为、命令、配置、安装步骤或使用方式发生变化时，必须更新 `README.md`。
- 只更新与本次变更直接相关的文档内容。
- 不顺手重写或美化无关文档段落。

## 工作完成标准

任务完成前，应确认以下事项：

- 已按需求完成最小必要改动。
- 未引入未请求的功能或抽象。
- 未修改无关代码、格式或文档。
- 已清理本次改动产生的无用代码。
- 已执行相关测试或说明无法执行的原因。
- 已根据需要更新 `README.md`。
- 已遵守文件删除安全限制。
- 已使用中文提交规范完成 Git commit。

## 成效判断

这些规则的目标是实现以下结果：

- 更少的不必要 diff。
- 更少的过度设计。
- 更少因误解需求导致的返工。
- 更清晰的验证过程。
- 更可靠、更可追踪的提交历史。
