# AGENTS.md

本文件补充项目级编码约束，适用于仓库中的全部新增和修改代码。

## 代码注释与接口文档

- 新增或修改公开类型、类、函数和接口方法时，必须使用 Doxygen 风格注释说明其职责。
- 对有入参的公开函数，必须说明每个入参的含义、有效范围以及所有权是否发生转移。
- 对有返回值的公开函数，必须说明成功结果、失败结果和返回对象的生命周期；错误码不能只写枚举名而不解释触发条件。
- 回调、异步接口和 move-only 资源必须说明调用时序、调用次数、所有权归属以及拒绝或失败后的资源去向。
- 复杂实现流程必须在关键决策点说明原因、不变量和回滚行为，例如“先持久化 Accepted，再派发工作”。
- 数据结构中的字段若不能从名称直接确定单位、坐标、索引范围或语义，必须为字段补充注释。
- 修改行为时必须同步检查并更新相关注释，禁止保留与实现不一致的过期描述。
- 不为简单赋值、机械转发或能由代码直接读出的语句添加逐行复述式注释；注释应解释契约和原因，而不是翻译代码。
